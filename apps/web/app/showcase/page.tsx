import { StoryTreeShowcase } from "@repo/ui";

export default function ShowcasePage() {
  return (
    <main className="min-h-screen bg-[var(--surface-base)]">
      <StoryTreeShowcase />
    </main>
  );
}
